import React from 'react'

const MainTitle = ({MainTitle}) => {
  return (
    <h2
      style={{
        textAlign: "center",
        margin: "auto",
        fontSize: "60px",
        marginTop: "50px",
      }}
    >
      {MainTitle}
    </h2>
  );
}

export default MainTitle